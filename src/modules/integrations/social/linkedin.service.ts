/**
 * REMOVED FROM SCOPE — Master Plan v1.1 (Section 8 changelog v1.0 → v1.1).
 *
 * Previously scoped to posting + sign-in only, because LinkedIn's Marketing
 * API Terms prohibit using member data "to enhance customer data in a CRM or
 * marketing automation platform" — close to this platform's core purpose.
 * Rather than maintain a permanently scope-limited integration, LinkedIn was
 * removed from the plan entirely as of v1.1.
 *
 * This file is kept only so that reasoning is documented, not silently
 * dropped. It can be deleted once nothing references it — Windows Controlled
 * Folder Access blocked this session from deleting it directly; `rm` or your
 * editor's delete will work fine for you.
 */
export {};
