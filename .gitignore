node_modules/
dist/
.env

# Real tenant-uploaded Catalog/Deal images (local-disk storage — see
# src/common/uploads.ts's own comment on why, and its disclosed limitation).
# Never committed: this is real user content, not source.
uploads/

# Terraform: .terraform.lock.hcl IS committed (Terraform's own convention);
# everything else here is local working state or holds real infra
# secrets/state and must never reach source control.
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
infra/terraform/*.tfvars
infra/terraform/crash.log

# terraform-bootstrap's own state holds the S3 bucket/DynamoDB table it
# creates — same reasoning as infra/terraform above, and this stack's
# state stays local by design (it's what bootstraps the other stack's
# remote backend, so it can't use that backend itself).
infra/terraform-bootstrap/.terraform/
infra/terraform-bootstrap/*.tfstate
infra/terraform-bootstrap/*.tfstate.*
infra/terraform-bootstrap/*.tfvars
infra/terraform-bootstrap/crash.log
