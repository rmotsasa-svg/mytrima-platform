# Base URL of the running Mytrima backend (src/main.ts). The backend must
# have CORS_ORIGIN set to this SPA's own origin (e.g. http://localhost:5173)
# or every request from here is silently blocked by the browser itself —
# see src/common/cors.ts in the main repo.
VITE_API_BASE_URL=http://localhost:3000
